name: Build C# GUI App

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  run-script:
      runs-on: windows-latest
      steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Doxygen from .exe to specific directory
        run: |
            .\doxygen-installer.exe /quiet /install INSTALLDIR="C:\doxygen"

      - name: Find Doxygen installation path
        run: |
            $doxygenPath = "C:\doxygen\bin\doxygen.exe"
            if (Test-Path $doxygenPath) {
              Write-Host "Doxygen is installed at $doxygenPath"
              $env:PATH += ";C:\doxygen\bin"
            } else {
              Write-Host "Doxygen not found at the expected location."
              exit 1

      - name: Check if Doxygen is installed
        run: doxygen --version

      - name: Check if Doxygen is installed
        run: doxygen --version

      


